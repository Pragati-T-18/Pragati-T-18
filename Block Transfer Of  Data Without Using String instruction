DATA SEGMENT
   XYZ DB 11,12,13,14,15
   PQR DB 5 DUP(0)
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START:
  MOV DX,DATA
  MOV DS,DX
  LEA SI,XYZ
  LEA DI,PQR
  MOV CL,05
AGAIN: 
  MOV AL,[SI]
  MOV [DI],AL
  INC SI
  INC DI
  DEC CL
  JNZ AGAIN

MOV AH,4CH
INT 21H

CODE ENDS
END START
